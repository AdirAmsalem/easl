{
  "name": "@easl/worker",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "build": "wrangler deploy --dry-run --outdir=dist",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "db:migrate": "wrangler d1 execute easl-db --file=./schema.sql"
  },
  "dependencies": {
    "@resvg/resvg-wasm": "^2.6.2",
    "aws4fetch": "^1.0.20",
    "fflate": "^0.8.2",
    "hono": "^4.7.0",
    "satori": "^0.26.0",
    "uqr": "^0.1.2"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260317.1",
    "typescript": "^5.7.0",
    "wrangler": "^4.77.0"
  }
}
