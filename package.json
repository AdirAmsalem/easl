{
  "name": "easl",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/AdirAmsalem/easl.git"
  },
  "description": "Instant hosting for AI agents — turn output into pages worth sharing",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "typecheck": "turbo typecheck",
    "deploy": "turbo deploy",
    "test": "vitest run",
    "test:e2e": "vitest run -c packages/worker/vitest.config.e2e.mts"
  },
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.14.1",
    "turbo": "^2.8.20",
    "vitest": "^4.1.0"
  }
}
